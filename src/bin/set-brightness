#!/bin/sh
# args: up|down

current=$(brightnessctl g)
max=$(brightnessctl m)
step=$((max * 5 / 100))

if [ $1 = 'up' ]; then
  if [ $(($current + $step)) -lt $max ]; then brightnessctl set 5%+; fi
else
  if [ $(($current - $step)) -gt 0 ]; then brightnessctl set 5%-; fi
fi
